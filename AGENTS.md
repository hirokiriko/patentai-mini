# AGENTS.md

本プロジェクトに参加する AI エージェント向けの作業規約です。

## ミッション
このプロジェクトの目的は、特許案と既存特許群の比較から、**拒絶されやすい構成要素の早期発見**を支援することです。法的断定ではなく、**調査支援・比較支援・論点抽出**が主眼です。

## 絶対原則
1. **法的断定をしない**
   - 「登録可能」「拒絶されない」と断言しない
   - 「重複候補」「一致候補」「先行技術として危険」などの表現を使う
2. **請求項ベースで考える**
   - 明細書の全体要約だけで済ませない
   - 独立請求項と従属請求項を分けて扱う
3. **構成要素を分解する**
   - 名詞句の列挙で終わらせず、要素・関係・制約・作用効果に分解する
4. **検索式は複数案出す**
   - 広め / 中庸 / 狭め の 3 系統を基本とする
5. **継続運用の痕跡を残す**
   - 作業完了時は必ず `ops/session-log.md` と `ops/handoff.md` を更新する

## 作業開始チェック
- `ops/handoff.md` を読む
- `ops/tasks.md` の In Progress を確認する
- `ops/decisions.md` の最新決定を確認する
- 直前セッションの `ops/session-log.md` を読む

## 作業終了チェック
- 完了したタスクを `ops/tasks.md` に反映
- 実施内容と未解決事項を `ops/session-log.md` に記録
- 次セッションが即着手できる状態で `ops/handoff.md` を更新
- 設計判断があれば `ops/decisions.md` に記録

## 推奨役割分担
- Analyst Agent: 特許文書分解、請求項要素抽出
- Search Agent: J-PlatPat 用検索式生成、検索観点整理
- Compare Agent: 既存特許との一致・差分抽出
- Report Agent: 人間向けレポート生成

## 禁止事項
- J-PlatPat の仕様を未確認のまま断定的に決めつけること
- PDF/CSV の列定義を推測だけで固定すること
- 類似度スコアだけで重複を断定すること
